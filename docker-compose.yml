
services:
  db:
    image: postgis/postgis:15-3.3
    container_name: prediction_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-prediction_engine}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./backend/db/migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-prediction_engine}"]
      interval: 10s
      timeout: 5s
      retries: 5

  n8n:
    image: n8nio/n8n
    container_name: prediction_n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - WEBHOOK_URL=http://localhost:5678/
      - EXECUTIONS_DATA_SAVE_ON_ERROR=all
      - EXECUTIONS_DATA_SAVE_ON_SUCCESS=all
      - EXECUTIONS_DATA_SAVE_ON_PROGRESS=true
      - GENERIC_TIMEZONE=Asia/Kolkata
    volumes:
      - n8n_data:/home/node/.n8n
      - ./n8n/workflow.json:/home/node/workflow.json

  # Auto-import workflow on startup
  n8n-import:
    image: n8nio/n8n
    container_name: prediction_n8n_import
    depends_on:
      - n8n
    volumes:
      - n8n_data:/home/node/.n8n
      - ./n8n/workflow.json:/home/node/workflow.json
    entrypoint: /bin/sh
    command: -c "sleep 10; n8n import:workflow --input=/home/node/workflow.json; exit 0"

volumes:
  pgdata:
    driver: local
  n8n_data:
